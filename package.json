{
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "main": "src/script.js",
  "scripts": {
    "build": "tsc",
    "predeploy": "yarn version --patch",
    "deploy": "yarn build && cross-var cavcli scripttest $npm_package_main $npm_package_name $npm_package_version -r \"$npm_package_cavrnus_testroom\""
  },
  "devDependencies": {
    "@cavrnus/cli": "^0.1.1",
    "@cavrnus/runtime": "^0.1.0",
    "cross-var": "^1.1.0",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  },
  "cavrnus": {
    "testroom": "{{testroom}}"
  }
}
